{% extends 'web/layout.html.twig' %}

{% block body %}

    Frontpage!

    {% if app.user %}
        <br>
        <a href="{{ logout_url('web') }}">Log out</a>
    {% endif %}

    {% if app.debug %}
        <br>
        <a href="{{ path('admin-home') }}">Admin panel</a>
    {% endif %}
    <br>
    <a href="{{ path('web-docs_api') }}">API documentation</a>

{% endblock %}
