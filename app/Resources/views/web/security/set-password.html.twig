{% extends 'web/layout.html.twig' %}

{% block body %}
    {% if app.user %}
    {% endif %}

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Set my new password</button>
    {{ form_end(form) }}
{% endblock %}
