<script>
    window.appData = {
        apiUrl: {{ (app.request.baseUrl~'/api/')|json_encode|raw }},
        logoutUrl: {{ logout_url('web')|json_encode|raw }},
        currentUser: {{ render(controller('AppBundle:Api/Api:me',{'include':'sites'})) }}.user
    }
</script>
